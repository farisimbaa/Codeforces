#include <bits/stdc++.h>
using namespace std;

struct Point {
    double x, y;
};



int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    Point p1, p2, p3;
    cin >> p1.x >> p1.y;
    cin >> p2.x >> p2.y;
    cin >> p3.x >> p3.y;
}